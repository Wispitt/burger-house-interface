import styled from 'styled-components';

export const IconAdd = styled.img`
    width: 3vw;
    margin: 0 10px 8px 0;
    transition: transform 0.6s ease;

    &:hover {
        transform: scale(1.2);
        cursor: pointer;
    }
`;
